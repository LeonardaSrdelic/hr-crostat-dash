---
title: "Gospodarski rast"
format:
  html:
    toc: true
    toc-title: "Sadržaj"
    number-sections: true
    fig-cap-location: bottom
execute:
  echo: false
  message: false
  warning: false
  error: false
---



```{r}
knitr::opts_chunk$set(fig.width = 10, fig.height = 6)
```

```{r}
# Inicijalni setup
source("R/indicators.R")

# Jednokratno učitaj podatke
dat   <- prepare_gdp_data()
panel <- build_panel_real(dat$gdp_real, dat$gdp_real_aggregates)
```


> Stranica se automatski ažurira ponedjeljkom u 06:00 (CET). Posljednje ažuriranje: `r format(lubridate::with_tz(Sys.time(), "Europe/Zagreb"), "%d.%m.%Y. u %H:%M")`.

```{=html}
<script>
if (!window.__calloutResizeInit) {
  window.__calloutResizeInit = true;
  document.addEventListener('shown.bs.collapse', function() {
    window.dispatchEvent(new Event('resize'));
  });
}
</script>
```


# Tromjesečni podaci realnog BDP-a

## mlrd. EUR realni BDP

```{r}
plot_gdp_real_q_level_eu27_tminus1_echarts()
```


## qoq stopa promjene

```{r}
plot_gdp_real_q_qoq_eu27_tminus1_echarts()
```


## qoq stopa promjene duža serija

```{r}
plot_gdp_qoq_hr()
```

## yoy stopa promjene

```{r}
plot_gdp_real_q_yoy_eu27_tminus1()
```

::: {.callout-note collapse=true title="Prikaži / sakrij graf (podaci privremeni)"}
Graf je privremen dok ne stignu svi podaci; klikni za prikaz ili skrivanje.

```{r}
plot_gdp_real_q_yoy_eu27_latest()
```
:::


# Godišnji podaci 
## realni BDP
```{r}
plot_gdp_real_growth_eu27_latest()
```

## nominalni BDP

```{r}
plot_gdp_nominal_growth_eu27_latest()
```

## BDP po stanovniku (PPS, EU 27 = 100)

```{r}
plot_gdp_pc_cee_pps_last()
```

# Dugoročna kretanja realnog BDP-a


```{r}
plot_gdp_index(panel, meta = dat)
```


```{r}
plot_gdp_yoy(panel, meta = dat)
```

## CEE 

```{r, fig.height=6}
plot_gdp_index_distribution_echarts(gdp_real = dat$gdp_real, panel_df = panel, group_key = "CEE_exHR", meta = dat)
plot_gdp_yoy_distribution_echarts(gdp_real = dat$gdp_real, panel_df = panel, group_key = "CEE_exHR", meta = dat)
```

## EU 27 

```{r, fig.height=6}
plot_gdp_index_distribution_echarts(gdp_real = dat$gdp_real, panel_df = panel, group_key = "EU27_exHR", meta = dat)
plot_gdp_yoy_distribution_echarts(gdp_real = dat$gdp_real, panel_df = panel, group_key = "EU27_exHR", meta = dat)
```

## Europodručje

```{r, fig.height=6}
plot_gdp_index_distribution_echarts(gdp_real = dat$gdp_real, panel_df = panel, group_key = "EA_exHR", meta = dat)
plot_gdp_yoy_distribution_echarts(gdp_real = dat$gdp_real, panel_df = panel, group_key = "EA_exHR", meta = dat)
```



# Napomene


<hr>

<div style="text-align:center; font-size:0.85em; color:grey; margin-top:1em;">
© Leonarda Srdelić, 2025
</div>
